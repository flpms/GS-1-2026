# Prompt de handoff para o Claude Code

Cole o bloco abaixo no Claude Code, rodando a partir da pasta
`/Users/filipe/Claude/Projects/Governança/GS2026_4M_NovaEconomiaEspacial`.

---

## Contexto do projeto

Estou na Global Solution 2026.1 da FIAP (disciplina de Governança em IA &
Business Analytics), Grupo 4M. O tema que costura tudo é a **Nova Economia
Espacial**: cada disciplina do semestre é um elo de uma cadeia de dados que vai
da órbita até a decisão.

Esta pasta já é um monorepo montado, organizado por disciplina. Preciso de você
para (1) publicar no GitHub e (2) continuar a entrega.

### Integrantes (definitivo)
- Bruno Lanchariche Fitipaldi Lopes — RM 564129
- Filipe Melo da Silva — RM 564571
- Rafael de Paiva Ramos — RM 563978

(Atenção: o README do projeto de PLN veio de outra turma e citava "Gustavo
Civita" e "Thiago Danyluk". Esses nomes já foram removidos; se encontrar
qualquer resquício deles no código ou em metadados, corrija.)

### Estado atual das pastas
- `01-rpa-spacewatch/` — só README, aponta para https://github.com/paivazzz/GS_RPA (repo já existente, não duplicar)
- `02-cluster-neuromorfica-neurospace/` — completo (NeuroSpace Alert: notebook + relatório + csv)
- `03-computacao-quantica-qml-satelite/` — completo (QML telemetria de satélite)
- `04-frontend-mobile-sentinela/` — só README, aponta para https://github.com/paivazzz/GS_Front (repo já existente, não duplicar)
- `05-genai-advanced-nets-spaceconnect/` — completo (RAG Space Connect)
- `06-physical-computing-iot/` — PENDENTE (sem material)
- `07-pln-chatbots-edificios-verdes/` — completo (chatbot RAG edifícios verdes)
- `08-visao-computacional/` — PENDENTE (sem material)
- `09-governanca/` — README pronto; falta o relatório ABNT
- `docs/` — roteiro do vídeo + este handoff

## Tarefa 1 — Publicar no GitHub

1. Inicializar git nesta pasta (se ainda não for repo), respeitando o `.gitignore` da raiz.
2. Criar um repositório novo no GitHub e dar push. Decidir comigo em qual conta
   (a minha, ou a `paivazzz` do Rafael, que já hospeda os outros dois repos).
   Se eu tiver o `gh` CLI autenticado, use `gh repo create`.
3. Os repos `paivazzz/GS_RPA` e `paivazzz/GS_Front` continuam separados, apenas
   linkados pelos READMEs. NÃO me adicionar como contribuidor neles e NÃO copiar
   o código deles para cá.
4. Depois do push, atualizar os campos "a publicar" no `README.md` da raiz e no
   roteiro (`docs/roteiro-video.md`) com a URL final do repositório.

## Tarefa 2 — Continuar a entrega

- Escrever o **relatório ABNT/FIAP** em `09-governanca/` (10 a 15 páginas):
  capa e folha de rosto com os integrantes, listas de quadros/figuras, sumário,
  introdução, desenvolvimento com um tópico por disciplina (terminando em
  Governança) e conclusão, mais um tópico "Links".
- Regras do enunciado, importantes: a **conclusão não pode ser gerada por IA**
  (só revisada); evitar jargão de IA, hipérbole, excesso de adjetivo e travessões.
- Faltam duas disciplinas (Physical Computing e Visão Computacional). Sem o
  material delas, o relatório fica incompleto: me lembrar disso.

## Estilo de escrita (quando for redigir texto meu)
Primeira pessoa, conversacional, frases costuradas na vírgula com a curta entrando
só como respiro, trazendo o leitor junto ("vamos", "precisamos"). Pouco travessão.
Direto, sem provocar briga. Em entrega formal (relatório), gramática limpa,
mantendo a voz.
